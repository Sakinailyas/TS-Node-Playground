//versin that passes
let alien_colour = "green";
if(alien_colour === "green"){
    console.log("You just earned 5 points:");
}
//version that fails (no output):
if(alien_colour === "white"){
    //no output because consition is false
}